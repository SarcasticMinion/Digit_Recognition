import torch
from torchvision datasets, transoforms
from torch impoty optim, nn
from torch.utils import data

class ConvNN(nn.Module):

    def __init__(self, optimzer, loss_fxn, neural_net, trainset)
